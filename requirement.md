# Vive Room Tracker — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Tested versions are pinned approximately; the project should work
# with any reasonably recent versions of these packages.

openvr>=1.16
numpy>=1.20
matplotlib>=3.5